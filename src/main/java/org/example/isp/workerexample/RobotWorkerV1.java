package org.example.isp.workerexample;

public class RobotWorkerV1 implements Workable{

    /**
     * By segregating the interfaces, we have ensured that
     * clients only implement the methods they actually need,
     * adhering to the ISP.
     */
    @Override
    public void work() {

    }
}
